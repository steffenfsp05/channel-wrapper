this project includes an simulated eventdriven pluginmessaging pipeline 
which will be wrapped around by the TransportService to manage (batching, efficiency).
this also can be used for websockets (implementation is planned)
this project is also used in omni service

look in omni client for a example usage (or will  later explain it here)
